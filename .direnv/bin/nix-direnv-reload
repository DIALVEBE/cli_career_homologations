#!/usr/bin/env bash
set -e
if [[ ! -d "/media/data/teach/intersemestral_2026/career_homologations/cli_career_homologations" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/media/data/teach/intersemestral_2026/career_homologations/cli_career_homologations")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/media/data/teach/intersemestral_2026/career_homologations/cli_career_homologations" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/media/data/teach/intersemestral_2026/career_homologations/cli_career_homologations/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/media/data/teach/intersemestral_2026/career_homologations/cli_career_homologations/.envrc" "/media/data/teach/intersemestral_2026/career_homologations/cli_career_homologations/.direnv"/*.rc
